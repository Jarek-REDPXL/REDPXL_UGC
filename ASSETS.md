# REDPXL UGC — Asset Drop-in Map

Everything on the site ships with **designed placeholders** (PosterCanvas
duotones, `TODO:REAL-DATA` copy, text wordmarks). Nothing fake is presented as
real (DESIGN.md §1.8). This file is the single checklist for swapping in real
assets before launch.

In **development** (`npm run dev`) every unfilled video slot shows a dashed red
**PLACEHOLDER** badge (bottom-right) so gaps are obvious. The badge never
renders in the production build.

> Find every open slot fast: `grep -rn "TODO:REAL-DATA" .`

---

## 1 · Video slots (13)

Drop MP4s into `public/videos/`, then pass `src` to the slot. All slots are
9:16 `PhoneFrame`s. Muted/looped/`playsInline`, `preload="none"`. Until a `src`
is set, the slot renders a `PosterCanvas` (per-niche duotone + UGC caption) with
a play button.

| # | Where | `chip` | Suggested file | Prop to set |
|---|-------|--------|----------------|-------------|
| 1 | Hero cluster (L) | `FITNESS · REELS` | `fitness.mp4` | `src` on `PhoneFrame` |
| 2 | Hero cluster (mid, caption cycles) | `SKINCARE · TIKTOK` | `skincare.mp4` | `src` on `PhoneFrame` |
| 3 | Hero cluster (R) | `SAAS · YOUTUBE` | `saas.mp4` | `src` on `PhoneFrame` |
| 4 | Work reel | `SKINCARE · TIKTOK` | `skincare.mp4` | `src` in `Work.tsx` map |
| 5 | Work reel | `SUPPLEMENTS · META` | `supplements.mp4` | ″ |
| 6 | Work reel | `FASHION · TIKTOK` | `fashion.mp4` | ″ |
| 7 | Work reel | `MOBILE APP · META` | `app.mp4` | ″ |
| 8 | Work reel | `BEAUTY · TIKTOK` | `beauty.mp4` | ″ |
| 9 | Work reel | `FITNESS · REELS` | `fitness.mp4` | ″ |
| 10 | Work reel | `FOOD & BEV · TIKTOK` | `food.mp4` | ″ |
| 11 | Work reel | `SAAS · YOUTUBE` | `saas.mp4` | ″ |
| 12 | Work reel | `PET CARE · TIKTOK` | `pet.mp4` | ″ |
| 13 | Work reel | `HOME & LIVING · REELS` | `home.mp4` | ″ |

> The Hero mobile view (`sm:hidden`) reuses slots 2 & 3 — no extra files.
> The Work reel duplicates its 10 phones for the seamless marquee; you only set
> `src` once per niche in the `CHIPS`/reel map.

**How to wire the reel:** in [Work.tsx](components/Work.tsx) the reel maps over
`CHIPS`. Change it to map over `{ chip, src }` objects and pass `src={item.src}`
to `<PhoneFrame>`. **How to wire the hero:** add `src="/videos/…​.mp4"` to each
`<PhoneFrame>` in [Hero.tsx](components/Hero.tsx) (lines ~105–127).

Optional: pass a `poster="/videos/….jpg"` first-frame image per slot for a
crisper initial paint.

---

## 2 · Images

| Asset | File | Notes |
|-------|------|-------|
| Logo (nav + footer) | `assets/icons/logo.webp` → rendered via `Logo` | already wired |
| Favicon | `app/icon.svg` + `favicon.webp` | already wired |
| OG image | `public/og.png` | generated by `node scripts/gen-og.mjs` |
| Client logos | text wordmarks in [LogoStrip.tsx](components/LogoStrip.tsx) | swap for real client SVGs (`TODO:REAL-DATA`, line ~64) |

---

## 3 · Copy / numbers to verify (`TODO:REAL-DATA`)

| Where | What | File |
|-------|------|------|
| Pricing | `£750` trial + `£2000 / £3500 / £6000` plans | [Pricing.tsx](components/Pricing.tsx) |
| Results | headline metrics + testimonial quotes | [Results.tsx](components/Results.tsx) (lines ~84, ~110) |
| Comparison | cost/turnaround figures | [Comparison.tsx](components/Comparison.tsx) (line ~92) |
| LogoStrip | client stat chips (`+212% ROAS` etc.) | [LogoStrip.tsx](components/LogoStrip.tsx) (line ~15) |
| Guarantee | trial terms | [Guarantee.tsx](components/Guarantee.tsx) (line ~23) |
| Graphics | BatchDrop sample tiles/filenames | [BatchDrop.tsx](components/graphics/BatchDrop.tsx) — illustrative only |

---

## 4 · Links / contact

All in [lib/site.ts](lib/site.ts):

| Constant | Current value | Action |
|----------|---------------|--------|
| `bookingUrl` | `https://calendly.com/meeting-redpxl` | confirm real booking link |
| `email` | `hello@redpxl.uk` | confirm |
| `supportEmail` | `support@redpxl.uk` | confirm |
| `location` | `Canary Wharf, London` | confirm |

---

## Launch checklist

- [ ] 13 MP4s in `public/videos/`, every `PhoneFrame`/`VideoSlot` has a `src`
- [ ] No dashed **PLACEHOLDER** badges remain in `npm run dev`
- [ ] Real client logos in `LogoStrip.tsx`
- [ ] All `TODO:REAL-DATA` numbers verified or removed
- [ ] `bookingUrl` + contact details confirmed in `lib/site.ts`
- [ ] `node scripts/gen-og.mjs` re-run if the headline changed
- [ ] `npm run build` clean · `npm run audit:shots` → zero overflow
